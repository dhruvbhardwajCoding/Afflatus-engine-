node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.env

# Local / optional Firebase client config (use VITE_FIREBASE_* instead)
firebase-applet-config.json

# Optional metadata files for Firebase Blueprint (used for deployment)
firebase-blueprint.json
metadata.json

# Local JSON DB 
data/


bun.lock


